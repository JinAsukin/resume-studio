# 统一换行符，避免 Windows / macOS 协作时产生整文件 diff 噪音
* text=auto eol=lf

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.docx binary

# Windows 脚本保留 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
